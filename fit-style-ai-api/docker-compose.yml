services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: fitstyle
      POSTGRES_PASSWORD: fitstyle
      POSTGRES_DB: fitstyle
    ports:
      - "5432:5432"
    volumes:
      - fitstyle_pg:/var/lib/postgresql/data

volumes:
  fitstyle_pg:
