# PostgreSQL (로컬: docker compose 또는 직접 설치)
DATABASE_URL="postgresql://fitstyle:fitstyle@localhost:5432/fitstyle?schema=public"

PORT=3001

# 관리자 API (/v1/admin/items, /v1/admin/outfits) — 8자 이상 랜덤 문자열 권장
# Authorization: Bearer <값> 또는 X-Admin-Key: <값>
ADMIN_API_KEY=
